# Shortly - Minimal URL Shortener

-> This is a minimal Next.js (app dir) + Prisma + SQLite URL shortener.
-> To run locally:
  1. npm install
  2. npx prisma migrate dev --name init
  3. npm run dev

Deploy to Vercel: push to GitHub and connect the repo in Vercel.
